import { ZoomElementDirective } from './zoom-element.directive';

describe('ZoomElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomElementDirective();
    expect(directive).toBeTruthy();
  });
});
