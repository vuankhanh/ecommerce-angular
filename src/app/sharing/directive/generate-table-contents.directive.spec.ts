import { GenerateTableContentsDirective } from './generate-table-contents.directive';

describe('GenerateTableContentsDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateTableContentsDirective();
    expect(directive).toBeTruthy();
  });
});
